.ml(*ngIf="enode")
  p
    | Action: {{enode.data.action}}
  
  ul.logic-children
    li(*ngFor="let d of enode.children") 
      match-view(*ngIf="d.type == 'match'", [enode]="d")
      match-logic-view(*ngIf="d.type == 'match-logic'", [enode]="d")
