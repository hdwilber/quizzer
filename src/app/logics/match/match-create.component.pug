.input-row(*ngIf="root")
  label
    | Operator
  select.form-control([(ngModel)]="tmp.operator")
    option(disabled) 
      | Select an Action
    option(*ngFor="let op of getOperatorTypes()", [ngValue]="op")
      | {{op.name}}

  label
    | Select target
  .catalog-wrapper
    enode([enode]="root", [current]="tmp.target", (seleted)="handleSelectedTarget($event)")


